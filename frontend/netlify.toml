[build]
  command = "npm run build"
  publish = "dist"  # Carpeta donde Vite almacena la build

[[redirects]]
  from = "/api/*"
  to = "${VITE_API_BASE_URL}/:splat"
  status = 200
  force = true

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200